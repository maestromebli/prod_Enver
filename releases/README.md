# Релізи клієнтів і модулів

- **Виробничий модуль → ENVER OS:** `npm run pack:production-module` створює `releases/enver-production-module.zip`.
- **Android:** `npm run build:android-client` збирає `releases/enver-operator-android.apk` для завантаження з **Налаштування → Клієнти**.
