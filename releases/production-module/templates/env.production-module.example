# Фрагмент .env для виробничого модуля ENVER OS
# Додайте до основного .env на сервері (/opt/enver/.env)

NODE_ENV=production
PORT=3000

# Postgres (обов'язково)
DATABASE_URL=postgres://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL_MIGRATIONS=postgres://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# Секрет сесій (обов'язковий у production)
SESSION_SECRET=замініть-на-випадковий-рядок

# Файли конструктивів (у Docker: /data/uploads)
UPLOADS_DIR=/data/uploads

# ШІ-аналіз конструктивів (опційно; можна задати в UI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Домен для Caddy
DOMAIN=enver.example.com
